comp += [ "-warn-error" "A" ]

begin program "opam-to-json"
  files = [
    "opam_to_json.ml"
  ]
  requires = [ "opam-client"; "opam-core" ]
end


begin program "opam-install-only"
  files = [
    "opam_install_only.ml"
  ]
  requires = [ "opam-client"; "opam-core" ]
end
